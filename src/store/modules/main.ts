import { createSlice } from '@reduxjs/toolkit'
const MainSlice = createSlice({
                name: 'Main',
initialState: {
},
reducers: {
}
})
export default MainSlice.reducer
export const {

} = MainSlice.actions